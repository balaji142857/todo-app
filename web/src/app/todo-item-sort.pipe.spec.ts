import { TodoItemSortPipe } from './todo-item-sort.pipe';

describe('TodoItemSortPipe', () => {
  it('create an instance', () => {
    const pipe = new TodoItemSortPipe();
    expect(pipe).toBeTruthy();
  });
});
